/**
 * define the common enum
 */
export enum USER_TYPE {
  Teacher = 'teacher',
  Student = 'student',
}

export enum FORM_COMPONENT {
  Input = 'Input',
  Select = 'Select',
  InputNumber = 'InputNumber',
}
